      tags:
        - name: Analisis data tweet
      consumes:
        - multipart/form-data
      parameters:
        - name: analisis
          in: formData
          required: true
          enum: [ "yes", "no"]
          type: string
       
          
      responses:
          '200':
              description : Successful responses
          '400':
              description : Bad Request
          '500':
              description : Internal Server Error